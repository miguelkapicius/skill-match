---
import { Building2Icon, MessageSquareTextIcon, UsersIcon } from "lucide-react";
import TypographyH1 from "./TypographyH1.astro";
import { Badge } from "./ui/badge";
import {
  Card,
  CardHeader,
  CardTitle,
  CardContent,
  CardDescription,
} from "./ui/card";
import { Image } from "astro:assets";
---

<section class="py-24 px-6" id={"social-impact"}>
  <main class="max-w-5xl mx-auto w-full">
    <div class="flex flex-col items-center justify-center gap-6 mb-24">
      <Badge variant={"outline"}>Impacto Social</Badge>
      <TypographyH1 text="tecnologia que transforma pessoas — e sociedades" />
    </div>

    <div class="flex flex-col gap-6">
      <Card>
        <CardHeader className="flex flex-col gap-6">
          <blockquote class="mt-6 border-l-2 pl-6 italic md:text-2xl flex-1">
            &quot;Antes, eu era operador de máquinas. Com a requalificação da
            SkillMatch, aprendi fundamentos de mecatrônica e controle
            industrial. Hoje, sou responsável por monitorar e ajustar os
            sistemas automatizados da fábrica. Em vez de ser substituído, evoluí
            junto com a tecnologia.&quot;
          </blockquote>
          <CardDescription className="w-max">
            — Sr. Mahindro, ex-aluno SkillMatch
          </CardDescription>
        </CardHeader>
      </Card>
      <Card className="relative">
        <CardContent
          className="flex flex-col md:flex-row items-center justify-between gap-y-6 md:w-3/4"
        >
          <div class="flex flex-col gap-4 items-center">
            <div class="p-4 rounded bg-[#6FB6E2]">
              <MessageSquareTextIcon className="size-5 md:size-8 lg:size-12" />
            </div>
            <CardTitle className="text-3xl lg:text-5xl">+10 mil</CardTitle>
            <CardDescription className="text-lg md:text-xl">
              Feedbacks Positivos
            </CardDescription>
          </div>
          <div class="flex flex-col gap-4 items-center">
            <div class="p-4 rounded bg-[#6FB6E2]">
              <Building2Icon className="size-5 md:size-8 lg:size-12" />
            </div>
            <CardTitle className="text-3xl lg:text-5xl">80</CardTitle>
            <CardDescription className="text-lg md:text-xl">
              Cidades Impactadas
            </CardDescription>
          </div>
          <div class="flex flex-col gap-4 items-center">
            <div class="p-4 rounded bg-[#6FB6E2]">
              <UsersIcon className="size-5 md:size-8 lg:size-12" />
            </div>
            <CardTitle className="text-3xl lg:text-5xl">72%</CardTitle>
            <CardDescription className="text-lg md:text-xl">
              Usuários requalificados
            </CardDescription>
          </div>
        </CardContent>

        <Image
          src={"/siri.webp"}
          alt="Foto de perfil"
          class="absolute -right-20 -top-20 hidden md:block md:w-52 lg:w-96"
          draggable="false"
          loading={"lazy"}
          width={400}
          height={400}
          format="webp"
        />
      </Card>
    </div>
  </main>
</section>
